%p= link_to 'Add new Vacancy', new_admin_vacancy_path, method: :get, class: "btn btn-info new_vacancy"
%table.table.table-striped.table-bordered
  %thead
    %tr
      %th Job Title
      %th Company
      %th Country
      %th City
      %th New/Accept/Reject
      =content_tag(:th,'Author') if current_user.admin
      %th Deadline
      %th
  %tbody
    - vacancies.each do |vacancy|
      %tr
        %td= link_to vacancy.title, admin_vacancy_path(vacancy)
        %td= vacancy.company.name
        %td= vacancy.country
        %td= vacancy.city
        %td
          = link_to admin_vacancy_summaries_path(vacancy) do
            %span.new_summary= vacancy.summaries.received.size
            \/
            %span= vacancy.summaries.accepted.size
            \/
            %span= vacancy.summaries.rejected.size
        =content_tag(:td,vacancy.creator.email) if current_user.admin
        %td{class: vacancy.status}= vacancy.deadline.to_s(:human)
        %td= link_to 'delete',admin_vacancy_path(vacancy),method: :delete,data: { confirm: 'Are you sure?' },class: 'btn btn-sm btn-danger'
