.col-md-6
  = render 'shared/error_msg', object: new_company
  = form_for new_company,url: admin_companies_path, method: :post  do |f|
    = render 'form', f: f
    .form-group
      = f.submit 'ADD', class: 'btn btn-success'
