%p= link_to 'Back', manage_vacancy_path(vacancy),class: 'btn btn-sm btn-primary'
.col-md-6
  = render 'shared/error_msg', object: vacancy
  = form_for vacancy,url: manage_vacancy_path, method: :put  do |f|
    = render 'form', f: f
    .form-group
      = f.submit 'Save', class: 'btn btn-success'
