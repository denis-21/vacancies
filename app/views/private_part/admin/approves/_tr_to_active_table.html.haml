%tr
  %td= link_to company.name, admin_company_path(company)
  %td= company.link
  %td= company.vacancies.size
  %td= link_to 'delete', admin_company_path(company), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-sm btn-danger'
